{{ if .Values.defaultStorageclassCluster2.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: default-storageclass-cluster2
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ .Values.defaultStorageclassCluster2.git.repoURL }}
    targetRevision: {{ .Values.defaultStorageclassCluster2.git.targetRevision }}
    path: {{ .Values.defaultStorageclassCluster2.git.path }}
  destination:
    name: {{ .Values.secondaryCluster.name }}
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
