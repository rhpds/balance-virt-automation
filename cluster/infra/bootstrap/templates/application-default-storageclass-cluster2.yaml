{{ if .Values.defaultStorageclass.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: default-storageclass-cluster2
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ .Values.defaultStorageclass.git.repoURL }}
    targetRevision: {{ .Values.defaultStorageclass.git.targetRevision }}
    path: {{ .Values.defaultStorageclass.git.path }}
  destination:
    name: sno1
    namespace: openshift-gitops
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
