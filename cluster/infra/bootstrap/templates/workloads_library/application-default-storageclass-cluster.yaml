{{ if .Values.defaultStorageclassCluster.enabled -}}
{{- range $dest := .Values.defaultStorageclassCluster.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: default-storageclass{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ $.Values.defaultStorageclassCluster.git.repoURL }}
    targetRevision: {{ $.Values.defaultStorageclassCluster.git.targetRevision }}
    path: {{ $.Values.defaultStorageclassCluster.git.path }}
  destination:
    name: {{ $dest }}
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
