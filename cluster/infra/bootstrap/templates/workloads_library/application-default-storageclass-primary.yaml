{{ if .Values.defaultStorageclassPrimary.enabled -}}
{{- range $dest := .Values.defaultStorageclassPrimary.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: default-storageclass{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ $.Values.defaultStorageclassPrimary.git.repoURL }}
    targetRevision: {{ $.Values.defaultStorageclassPrimary.git.targetRevision }}
    path: {{ $.Values.defaultStorageclassPrimary.git.path }}
  destination:
    name: {{ $dest }}
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
