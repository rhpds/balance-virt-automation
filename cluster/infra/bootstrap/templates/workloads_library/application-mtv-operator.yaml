{{ if .Values.mtvOperator.enabled -}}
{{- range $dest := .Values.mtvOperator.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mtv-operator{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: infra
  source:
    repoURL: {{ $.Values.mtvOperator.git.repoURL }}
    targetRevision: {{ $.Values.mtvOperator.git.targetRevision }}
    path: {{ $.Values.mtvOperator.git.path }}
  destination:
    name: {{ $dest }}
    namespace: openshift-mtv
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
