{{ if .Values.nodeHealthCheck.enabled -}}
{{- range $dest := .Values.nodeHealthCheck.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-health-check{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: platform
  source:
    repoURL: {{ $.Values.nodeHealthCheck.git.repoURL }}
    targetRevision: {{ $.Values.nodeHealthCheck.git.targetRevision }}
    path: {{ $.Values.nodeHealthCheck.git.path }}
  destination:
    name: {{ $dest }}
    namespace: openshift-workload-availability
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
