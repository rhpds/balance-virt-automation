{{ if .Values.odf.enabled -}}
{{- range $dest := .Values.odf.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: odf{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
spec:
  project: platform
  source:
    repoURL: {{ $.Values.odf.git.repoURL }}
    targetRevision: {{ $.Values.odf.git.targetRevision }}
    path: {{ $.Values.odf.git.path }}
  destination:
    name: {{ $dest }}
    namespace: default
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
