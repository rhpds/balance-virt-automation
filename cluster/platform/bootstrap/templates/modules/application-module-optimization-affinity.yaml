{{- if .Values.moduleOptimizationAffinity.enabled }}
{{- range $dest := .Values.moduleOptimizationAffinity.destinations }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: module-optimization-affinity{{- if ne $dest "in-cluster" }}-{{ $dest }}{{- end }}
  namespace: openshift-gitops
  labels:
    demo.redhat.com/application: ""
spec:
  project: default
  source:
    repoURL: {{ $.Values.moduleOptimizationAffinity.git.repoURL }}
    targetRevision: {{ $.Values.moduleOptimizationAffinity.git.targetRevision }}
    path: {{ $.Values.moduleOptimizationAffinity.git.path }}
  destination:
    name: {{ $dest }}
    namespace: affinity
  syncPolicy:
    automated:
      enabled: {{ $.Values.syncPolicy.automated.enabled }}
    retry:
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
    managedNamespaceMetadata:
      labels:
        openshift.io/cluster-monitoring: "true"
{{- end }}
{{- end }}
